declare module '*.css' {
  const text: string;
  export default text;
}

/** Source of src/layout/worker.ts, bundled and inlined by build.mjs. */
declare module 'orbit:layout-worker' {
  const source: string;
  export default source;
}
