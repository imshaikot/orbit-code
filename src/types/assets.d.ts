declare module '*.css' {
  const text: string;
  export default text;
}

/** Source of src/webview/layout/worker.ts, bundled and inlined by esbuild.mjs. */
declare module 'orbit:layout-worker' {
  const source: string;
  export default source;
}
