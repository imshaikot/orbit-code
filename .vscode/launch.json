{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Orbit Code: this repository",
      "type": "extensionHost",
      "request": "launch",
      "args": ["--extensionDevelopmentPath=${workspaceFolder}/apps/vscode", "--disable-extensions", "${workspaceFolder}"],
      "outFiles": ["${workspaceFolder}/apps/vscode/dist/**/*.js"],
      "preLaunchTask": "orbit: build"
    },
    {
      "name": "Orbit Code: this repository, reindexed",
      "type": "extensionHost",
      "request": "launch",
      "args": ["--extensionDevelopmentPath=${workspaceFolder}/apps/vscode", "--disable-extensions", "${workspaceFolder}"],
      "env": { "ORBIT_REINDEX": "1" },
      "outFiles": ["${workspaceFolder}/apps/vscode/dist/**/*.js"],
      "preLaunchTask": "orbit: build"
    }
  ]
}
